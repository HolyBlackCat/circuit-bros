#pragma once

#include "utils/mat.h"

namespace GuiColors
{
    inline constexpr fvec3 color_text(1), color_bg(0), color_border(0.2,0.5,1);
    inline constexpr float alpha_text = 1, alpha_bg = 0.5, alpha_border = 0.8;
}
